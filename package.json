{
  "name": "smart-contract-coffee-marketplace",
  "scripts": {
    "start:local:node": "npx hardhat node",
    "clean:local:node": "rm -rf ./hardhat-local-node",
    "compile": "npx hardhat compile --show-stack-traces",
    "clean": "npx hardhat clean",
    "test": "npx hardhat test",
    "deploy:local:node": "npx hardhat ignition deploy ./ignition/modules/Marketplace.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.8",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "chai": "4.3.7",
    "hardhat": "^2.22.17",
    "ethers": "^6.13.4"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-ignition": "^0.15.8",
    "@openzeppelin/contracts": "^5.1.0"
  }
}
